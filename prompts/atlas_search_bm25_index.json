{
  "name": "bm25_index",
  "mappings": {
    "dynamic": false,
    "fields": {
      "text": { "type": "string" },
      "skills": { "type": "string" },
      "role": { "type": "string" },
      "experienceSummary": { "type": "string" },
      "name": { "type": "string" },
      "education": { "type": "string" }
    }
  },
  "analyzer": "lucene.standard",
  "index": {
    "definition": {
      "type": "keyword"
    }
  },
  "notes": "Import this as an Atlas Search index named 'bm25_index' and map the above fields. This is a minimal example — adjust analyzers and field types in Atlas UI."
}
