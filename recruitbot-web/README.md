# RecruitBot Frontend (Phase 1)

This folder contains a minimal Vite + React + TypeScript scaffold used for Phase 1 (ingestion frontend scaffold).

To run:

```bash
cd recruitbot-web
npm install
npm run dev
```

Open http://localhost:5173 and confirm the "RecruitBot — Phase 1 (Ingestion Scaffold)" page loads.
